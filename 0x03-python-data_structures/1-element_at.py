#!/usr/bin/python3
def element_at(my_list, idx):
    for list in my_list:
        if idx < 0:
            return none
        if idx > len(my_list):
            return none
        else:
            return my_list[idx]
